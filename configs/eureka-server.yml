eureka:
  instance:
    hostname: localhost  # Docker service name
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/  # Docker service name

server:
  port: 8761
